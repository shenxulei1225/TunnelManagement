# 动态业务智能推荐开发指南

> **版本**: v1.0.0  
> **更新时间**: 2024-01-28  
> **适用开发者**: 后端开发人员、全栈开发人员

## 📋 开发概述

本文档为动态业务智能推荐系统的开发指南，包含架构设计、代码规范、开发流程和最佳实践。

## 🏗️ 系统架构

### 1. 分层架构

```
Controller Layer (控制器层)
    ↓
Service Layer (服务层)
    ↓
Mapper Layer (数据访问层)
    ↓
Database Layer (数据库层)
```

### 2. 核心模块

#### 推荐模块 (recommendation)
- **DynamicRecommendationService** - 智能推荐服务
- **DynamicRecommendationController** - 推荐控制器
- **DynamicRecommendationConvert** - 数据转换器

#### 预览模块 (preview)
- **DynamicPreviewService** - 预览服务
- **DynamicPreviewController** - 预览控制器

#### 数据对象 (dataobject)
- **DynamicSceneTemplateDO** - 场景模板数据对象
- **DynamicViewConfigDO** - 视图配置数据对象
- **DynamicUserBehaviorDO** - 用户行为数据对象

## 📁 项目结构

```
cheers-module-dynamic-business/
├── src/main/java/com/cheers/arch/module/dynamic/
│   ├── controller/admin/recommendation/
│   │   ├── DynamicRecommendationController.java
│   │   └── vo/
│   │       ├── SceneTemplateVO.java
│   │       ├── FieldRecommendationVO.java
│   │       ├── FieldMatchVO.java
│   │       └── ...
│   ├── service/recommendation/
│   │   ├── DynamicRecommendationService.java
│   │   └── impl/
│   │       └── DynamicRecommendationServiceImpl.java
│   ├── dal/dataobject/
│   │   ├── template/DynamicSceneTemplateDO.java
│   │   ├── view/DynamicViewConfigDO.java
│   │   └── behavior/DynamicUserBehaviorDO.java
│   ├── dal/mysql/
│   │   ├── template/DynamicSceneTemplateMapper.java
│   │   ├── view/DynamicViewConfigMapper.java
│   │   └── behavior/DynamicUserBehaviorMapper.java
│   └── convert/recommendation/
│       └── DynamicRecommendationConvert.java
└── src/main/resources/
    └── mapper/
        ├── template/DynamicSceneTemplateMapper.xml
        ├── view/DynamicViewConfigMapper.xml
        └── behavior/DynamicUserBehaviorMapper.xml
```

## 🔧 开发规范

### 1. 命名规范

#### 类命名
- **Controller**: `XxxController` - 控制器类
- **Service**: `XxxService` - 服务接口
- **ServiceImpl**: `XxxServiceImpl` - 服务实现类
- **Mapper**: `XxxMapper` - 数据访问接口
- **DO**: `XxxDO` - 数据对象
- **VO**: `XxxVO` - 视图对象
- **Convert**: `XxxConvert` - 数据转换器

#### 方法命名
- **查询**: `getXxx`、`selectXxx`、`findXxx`
- **创建**: `createXxx`、`insertXxx`
- **更新**: `updateXxx`、`modifyXxx`
- **删除**: `deleteXxx`、`removeXxx`
- **推荐**: `getRecommendedXxx`、`recommendXxx`

#### 字段命名
- **数据库字段**: 下划线命名法 (snake_case)
- **Java字段**: 驼峰命名法 (camelCase)
- **常量**: 全大写加下划线 (UPPER_SNAKE_CASE)

### 2. 代码规范

#### 注释规范
```java
/**
 * 根据业务类型获取推荐场景模板
 *
 * @param businessType 业务类型
 * @return 推荐场景模板列表
 */
public List<SceneTemplateVO> getRecommendedTemplates(String businessType) {
    // 实现逻辑
}
```

#### 异常处理
```java
try {
    // 业务逻辑
} catch (Exception e) {
    log.error("操作失败", e);
    throw new ServiceException("操作失败");
}
```

#### 参数验证
```java
@NotNull(message = "业务类型不能为空")
private String businessType;

@Valid
private List<FieldConfigVO> fields;
```

### 3. 数据库规范

#### 表命名
- 使用下划线命名法
- 以模块前缀开头
- 示例: `dynamic_scene_template`

#### 字段规范
- **主键**: `id BIGINT NOT NULL AUTO_INCREMENT`
- **租户字段**: `tenant_id BIGINT NOT NULL`
- **基础字段**: `creator`、`create_time`、`updater`、`update_time`、`deleted`
- **状态字段**: `status TINYINT NOT NULL DEFAULT 1`
- **排序字段**: `sort INT DEFAULT 100`

#### 索引规范
- **主键索引**: `PRIMARY KEY (id)`
- **唯一索引**: `UNIQUE KEY uk_xxx (field1, field2)`
- **普通索引**: `KEY idx_xxx (field1, field2)`

## 🚀 开发流程

### 1. 功能开发流程

#### 第一步：数据库设计
1. 设计数据表结构
2. 编写建表SQL脚本
3. 创建数据对象类 (DO)

#### 第二步：数据访问层
1. 创建Mapper接口
2. 编写XML映射文件
3. 实现基础CRUD操作

#### 第三步：业务逻辑层
1. 创建Service接口
2. 实现Service实现类
3. 编写业务逻辑

#### 第四步：控制器层
1. 创建Controller类
2. 定义API接口
3. 实现请求处理逻辑

#### 第五步：数据转换层
1. 创建VO类
2. 创建Convert接口
3. 实现数据转换逻辑

### 2. 推荐功能开发示例

#### 创建场景模板功能

**1. 数据对象类**
```java
@TableName("dynamic_scene_template")
@Data
@EqualsAndHashCode(callSuper = true)
public class DynamicSceneTemplateDO extends TenantBaseDO {
    @TableId
    private Long id;
    private String code;
    private String name;
    private String businessType;
    private String description;
    private String recommendedFields;
    private String defaultLayout;
    private String validationRules;
    private String icon;
    private Integer sort;
    private Integer status;
}
```

**2. Mapper接口**
```java
@Mapper
public interface DynamicSceneTemplateMapper extends BaseMapperX<DynamicSceneTemplateDO> {
    
    default List<DynamicSceneTemplateDO> selectByBusinessType(String businessType) {
        return selectList(new LambdaQueryWrapperX<DynamicSceneTemplateDO>()
                .eq(DynamicSceneTemplateDO::getBusinessType, businessType)
                .eq(DynamicSceneTemplateDO::getStatus, 1)
                .orderByAsc(DynamicSceneTemplateDO::getSort));
    }
    
    default DynamicSceneTemplateDO selectByCode(String code) {
        return selectOne(new LambdaQueryWrapperX<DynamicSceneTemplateDO>()
                .eq(DynamicSceneTemplateDO::getCode, code));
    }
}
```

**3. Service接口**
```java
public interface DynamicRecommendationService {
    
    /**
     * 根据业务类型获取推荐场景模板
     */
    List<SceneTemplateVO> getRecommendedTemplates(String businessType);
    
    /**
     * 根据场景模板推荐字段
     */
    List<FieldRecommendationVO> getFieldRecommendations(String templateCode);
}
```

**4. Service实现类**
```java
@Service
@Slf4j
public class DynamicRecommendationServiceImpl implements DynamicRecommendationService {
    
    @Resource
    private DynamicSceneTemplateMapper sceneTemplateMapper;
    
    @Resource
    private ObjectMapper objectMapper;
    
    @Override
    public List<SceneTemplateVO> getRecommendedTemplates(String businessType) {
        List<DynamicSceneTemplateDO> templates = sceneTemplateMapper.selectByBusinessType(businessType);
        
        return templates.stream()
                .filter(template -> template.getStatus() == 1)
                .map(this::convertToSceneTemplateVO)
                .sorted(Comparator.comparing(SceneTemplateVO::getSort))
                .collect(Collectors.toList());
    }
    
    @Override
    public List<FieldRecommendationVO> getFieldRecommendations(String templateCode) {
        DynamicSceneTemplateDO template = sceneTemplateMapper.selectByCode(templateCode);
        if (template == null) {
            return Collections.emptyList();
        }
        
        try {
            return objectMapper.readValue(
                    template.getRecommendedFields(), 
                    new TypeReference<List<FieldRecommendationVO>>() {}
            );
        } catch (Exception e) {
            log.error("解析推荐字段配置失败", e);
            return Collections.emptyList();
        }
    }
}
```

**5. Controller类**
```java
@Tag(name = "管理后台 - 动态推荐")
@RestController
@RequestMapping("/dynamic/recommendation")
@Validated
public class DynamicRecommendationController {
    
    @Resource
    private DynamicRecommendationService recommendationService;
    
    @GetMapping("/templates")
    @Operation(summary = "获取场景模板推荐")
    public CommonResult<List<SceneTemplateVO>> getRecommendedTemplates(
            @RequestParam("businessType") String businessType) {
        List<SceneTemplateVO> templates = recommendationService.getRecommendedTemplates(businessType);
        return success(templates);
    }
    
    @GetMapping("/fields")
    @Operation(summary = "获取字段推荐")
    public CommonResult<List<FieldRecommendationVO>> getFieldRecommendations(
            @RequestParam("templateCode") String templateCode) {
        List<FieldRecommendationVO> recommendations = recommendationService.getFieldRecommendations(templateCode);
        return success(recommendations);
    }
}
```

**6. VO类**
```java
@Data
@Schema(description = "管理后台 - 场景模板 VO")
public class SceneTemplateVO {
    
    @Schema(description = "模板ID", requiredMode = Schema.RequiredMode.REQUIRED)
    private Long id;
    
    @Schema(description = "模板编码", requiredMode = Schema.RequiredMode.REQUIRED)
    private String code;
    
    @Schema(description = "模板名称", requiredMode = Schema.RequiredMode.REQUIRED)
    private String name;
    
    @Schema(description = "业务类型", requiredMode = Schema.RequiredMode.REQUIRED)
    private String businessType;
    
    @Schema(description = "场景描述")
    private String description;
    
    @Schema(description = "推荐字段配置（JSON格式）")
    private String recommendedFields;
    
    @Schema(description = "默认布局配置")
    private String defaultLayout;
    
    @Schema(description = "验证规则配置")
    private String validationRules;
    
    @Schema(description = "图标")
    private String icon;
    
    @Schema(description = "排序")
    private Integer sort;
    
    @Schema(description = "状态", requiredMode = Schema.RequiredMode.REQUIRED)
    private Integer status;
    
    @Schema(description = "创建时间", requiredMode = Schema.RequiredMode.REQUIRED)
    private LocalDateTime createTime;
}
```

**7. Convert接口**
```java
@Mapper
public interface DynamicRecommendationConvert {
    
    DynamicRecommendationConvert INSTANCE = Mappers.getMapper(DynamicRecommendationConvert.class);
    
    SceneTemplateVO convert(DynamicSceneTemplateDO bean);
    
    List<SceneTemplateVO> convertList(List<DynamicSceneTemplateDO> list);
}
```

## 🧪 测试规范

### 1. 单元测试

#### 测试类命名
- 测试类以 `Test` 结尾
- 示例: `DynamicRecommendationServiceTest`

#### 测试方法命名
- 使用 `test` 前缀
- 描述测试场景
- 示例: `testGetRecommendedTemplates()`

#### 测试示例
```java
@SpringBootTest
class DynamicRecommendationServiceTest {
    
    @Resource
    private DynamicRecommendationService recommendationService;
    
    @Test
    void testGetRecommendedTemplates() {
        // Given
        String businessType = "inspection";
        
        // When
        List<SceneTemplateVO> templates = recommendationService.getRecommendedTemplates(businessType);
        
        // Then
        assertNotNull(templates);
        assertFalse(templates.isEmpty());
        templates.forEach(template -> {
            assertEquals(businessType, template.getBusinessType());
            assertEquals(1, template.getStatus());
        });
    }
    
    @Test
    void testGetFieldRecommendations() {
        // Given
        String templateCode = "inspection_daily";
        
        // When
        List<FieldRecommendationVO> recommendations = recommendationService.getFieldRecommendations(templateCode);
        
        // Then
        assertNotNull(recommendations);
        assertFalse(recommendations.isEmpty());
        recommendations.forEach(recommendation -> {
            assertNotNull(recommendation.getFieldCode());
            assertNotNull(recommendation.getFieldName());
            assertNotNull(recommendation.getFieldType());
        });
    }
}
```

### 2. 集成测试

#### API测试
```java
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@AutoConfigureTestDatabase(replace = AutoConfigureTestDatabase.Replace.NONE)
class DynamicRecommendationControllerTest {
    
    @Autowired
    private TestRestTemplate restTemplate;
    
    @Test
    void testGetRecommendedTemplates() {
        // Given
        String businessType = "inspection";
        
        // When
        ResponseEntity<CommonResult> response = restTemplate.getForEntity(
                "/dynamic/recommendation/templates?businessType=" + businessType,
                CommonResult.class
        );
        
        // Then
        assertEquals(HttpStatus.OK, response.getStatusCode());
        CommonResult result = response.getBody();
        assertNotNull(result);
        assertEquals(0, result.getCode());
        
        List<SceneTemplateVO> templates = (List<SceneTemplateVO>) result.getData();
        assertNotNull(templates);
        assertFalse(templates.isEmpty());
    }
}
```

## 📊 性能优化

### 1. 数据库优化

#### 索引优化
```sql
-- 为常用查询字段创建索引
CREATE INDEX idx_business_type ON dynamic_scene_template(business_type);
CREATE INDEX idx_status ON dynamic_scene_template(status);
CREATE INDEX idx_user_business ON dynamic_user_behavior(user_id, business_type);
```

#### 查询优化
```java
// 使用分页查询避免大数据量问题
public PageResult<SceneTemplateVO> getRecommendedTemplatesPage(String businessType, Integer pageNo, Integer pageSize) {
    Page<DynamicSceneTemplateDO> page = new Page<>(pageNo, pageSize);
    IPage<DynamicSceneTemplateDO> result = sceneTemplateMapper.selectPage(page, 
            new LambdaQueryWrapperX<DynamicSceneTemplateDO>()
                    .eq(DynamicSceneTemplateDO::getBusinessType, businessType)
                    .eq(DynamicSceneTemplateDO::getStatus, 1)
                    .orderByAsc(DynamicSceneTemplateDO::getSort));
    
    return new PageResult<>(convertList(result.getRecords()), result.getTotal());
}
```

### 2. 缓存优化

#### Redis缓存
```java
@Service
public class DynamicRecommendationServiceImpl implements DynamicRecommendationService {
    
    @Resource
    private RedisTemplate<String, Object> redisTemplate;
    
    @Override
    @Cacheable(value = "recommendation:templates", key = "#businessType")
    public List<SceneTemplateVO> getRecommendedTemplates(String businessType) {
        // 实现逻辑
    }
    
    @Override
    @Cacheable(value = "recommendation:fields", key = "#templateCode")
    public List<FieldRecommendationVO> getFieldRecommendations(String templateCode) {
        // 实现逻辑
    }
}
```

### 3. 异步处理

#### 用户行为记录
```java
@Async
public void recordUserBehaviorAsync(Long userId, String businessType, String sceneType, String modelCode, String fieldUsage) {
    try {
        recordUserBehavior(userId, businessType, sceneType, modelCode, fieldUsage);
    } catch (Exception e) {
        log.error("异步记录用户行为失败", e);
    }
}
```

## 🔒 安全规范

### 1. 权限控制

#### 接口权限
```java
@PreAuthorize("@ss.hasPermission('dynamic:recommendation:query')")
@GetMapping("/templates")
public CommonResult<List<SceneTemplateVO>> getRecommendedTemplates(String businessType) {
    // 实现逻辑
}
```

#### 数据权限
```java
// 在Service中过滤租户数据
public List<SceneTemplateVO> getRecommendedTemplates(String businessType) {
    List<DynamicSceneTemplateDO> templates = sceneTemplateMapper.selectByBusinessType(businessType);
    
    // 过滤当前租户的数据
    return templates.stream()
            .filter(template -> SecurityFrameworkUtils.getLoginUserId().equals(template.getCreator()))
            .map(this::convertToSceneTemplateVO)
            .collect(Collectors.toList());
}
```

### 2. 参数验证

#### 输入验证
```java
@GetMapping("/templates")
public CommonResult<List<SceneTemplateVO>> getRecommendedTemplates(
        @NotBlank(message = "业务类型不能为空")
        @RequestParam("businessType") String businessType) {
    // 实现逻辑
}
```

#### SQL注入防护
```java
// 使用参数化查询
public List<DynamicSceneTemplateDO> selectByBusinessType(String businessType) {
    return selectList(new LambdaQueryWrapperX<DynamicSceneTemplateDO>()
            .eq(DynamicSceneTemplateDO::getBusinessType, businessType));
}
```

## 📈 监控和日志

### 1. 日志规范

#### 日志级别
```java
// 错误日志
log.error("解析推荐字段配置失败", e);

// 警告日志
log.warn("用户行为数据格式异常: {}", fieldUsage);

// 信息日志
log.info("获取推荐模板成功，业务类型: {}, 数量: {}", businessType, templates.size());

// 调试日志
log.debug("开始计算字段匹配度，业务类型: {}, 场景类型: {}", businessType, sceneType);
```

#### 日志格式
```java
// 使用占位符而不是字符串拼接
log.info("用户 {} 访问推荐接口，业务类型: {}", userId, businessType);

// 记录关键操作
log.info("用户 {} 创建业务模型，模型编码: {}", userId, modelCode);
```

### 2. 性能监控

#### 接口监控
```java
@Timed(value = "recommendation.templates.duration", description = "获取推荐模板耗时")
@Counted(value = "recommendation.templates.count", description = "获取推荐模板次数")
@GetMapping("/templates")
public CommonResult<List<SceneTemplateVO>> getRecommendedTemplates(String businessType) {
    // 实现逻辑
}
```

#### 业务监控
```java
// 记录推荐成功率
@EventListener
public void handleRecommendationEvent(RecommendationEvent event) {
    if (event.isSuccess()) {
        meterRegistry.counter("recommendation.success").increment();
    } else {
        meterRegistry.counter("recommendation.failure").increment();
    }
}
```

## 🔧 部署和运维

### 1. 配置文件

#### application.yml
```yaml
# 推荐服务配置
dynamic:
  recommendation:
    # 缓存配置
    cache:
      enabled: true
      ttl: 3600
    # 异步配置
    async:
      enabled: true
      pool-size: 10
    # 监控配置
    monitor:
      enabled: true
      metrics: true
```

### 2. 数据库脚本

#### 初始化脚本
```sql
-- 创建推荐相关表
CREATE TABLE dynamic_scene_template (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '模板ID',
    code VARCHAR(64) NOT NULL COMMENT '模板编码',
    name VARCHAR(128) NOT NULL COMMENT '模板名称',
    business_type VARCHAR(64) NOT NULL COMMENT '业务类型',
    description TEXT COMMENT '场景描述',
    recommended_fields JSON COMMENT '推荐字段配置',
    default_layout JSON COMMENT '默认布局配置',
    validation_rules JSON COMMENT '验证规则配置',
    icon VARCHAR(64) COMMENT '图标',
    sort INT DEFAULT 100 COMMENT '排序',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态（0:禁用，1:启用）',
    creator VARCHAR(64) COMMENT '创建者',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updater VARCHAR(64) COMMENT '更新者',
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    tenant_id BIGINT NOT NULL COMMENT '租户编号',
    PRIMARY KEY (id),
    UNIQUE KEY uk_code (code),
    KEY idx_business_type (business_type),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='动态场景模板表';
```

### 3. 健康检查

#### 健康检查接口
```java
@Component
public class RecommendationHealthIndicator implements HealthIndicator {
    
    @Resource
    private DynamicSceneTemplateMapper sceneTemplateMapper;
    
    @Override
    public Health health() {
        try {
            long count = sceneTemplateMapper.selectCount(null);
            return Health.up()
                    .withDetail("templateCount", count)
                    .withDetail("status", "UP")
                    .build();
        } catch (Exception e) {
            return Health.down()
                    .withDetail("error", e.getMessage())
                    .withDetail("status", "DOWN")
                    .build();
        }
    }
}
```

## 📚 常见问题

### 1. MapStruct编译问题

**问题**: MapStruct无法生成实现类
**解决方案**:
```java
// 确保使用正确的注解
@Mapper(componentModel = "spring")
public interface DynamicRecommendationConvert {
    // 接口方法
}

// 或者使用工厂方法
@Mapper
public interface DynamicRecommendationConvert {
    DynamicRecommendationConvert INSTANCE = Mappers.getMapper(DynamicRecommendationConvert.class);
}
```

### 2. JSON解析问题

**问题**: JSON字符串解析失败
**解决方案**:
```java
try {
    List<FieldRecommendationVO> recommendations = objectMapper.readValue(
            jsonString, 
            new TypeReference<List<FieldRecommendationVO>>() {}
    );
} catch (Exception e) {
    log.error("JSON解析失败", e);
    return Collections.emptyList();
}
```

### 3. 租户ID问题

**问题**: 租户ID字段没有默认值
**解决方案**:
```sql
-- 在插入数据时提供租户ID
INSERT INTO dynamic_scene_template (code, name, business_type, tenant_id) 
VALUES ('template_code', '模板名称', 'business_type', 1);
```

### 4. 性能问题

**问题**: 查询性能慢
**解决方案**:
```java
// 1. 添加合适的索引
// 2. 使用分页查询
// 3. 启用缓存
// 4. 优化SQL查询
```

---

**📋 开发标准**: 所有新功能开发都应遵循本文档的规范和最佳实践，确保代码质量和系统稳定性。 