<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cheers.arch.module.system.dal.mysql.category.CategoryMapper">

    <!-- 根据父节点ID查询子节点 -->
    <select id="selectByParentId" resultType="com.cheers.arch.module.system.dal.dataobject.category.CategoryDO">
        SELECT * FROM system_category 
        WHERE deleted = 0 
        AND parent_id = #{parentId}
        ORDER BY sort ASC, id ASC
    </select>

    <!-- 根据编码查询节点 -->
    <select id="selectByCode" resultType="com.cheers.arch.module.system.dal.dataobject.category.CategoryDO">
        SELECT * FROM system_category 
        WHERE deleted = 0 
        AND code = #{code}
        LIMIT 1
    </select>

    <!-- 根据父节点ID和名称查询节点 -->
    <select id="selectByParentIdAndName" resultType="com.cheers.arch.module.system.dal.dataobject.category.CategoryDO">
        SELECT * FROM system_category 
        WHERE deleted = 0 
        AND parent_id = #{parentId}
        AND name = #{name}
        LIMIT 1
    </select>

    <!-- 统计子节点数量 -->
    <select id="selectCountByParentId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM system_category 
        WHERE deleted = 0 
        AND parent_id = #{parentId}
    </select>

</mapper> 