# 业务建模工作台字段需求分析

## 1. 错误分析

### 运行时错误
```
workspace.vue:596 获取模型列表失败: Error: 系统异常
```

**问题原因**：前端已从 `dynamic/model` API 迁移至 `system/domain` API，但后端 API 接口可能还未完全准备好或参数不匹配。

### 前端 API 调用分析
当前前端使用的 `DomainApi.getDomainPage()` 调用：
```typescript
const fetchModels = async () => {
  try {
    const resp = await DomainApi.getDomainPage({ 
      pageNo: 1, 
      pageSize: 100,
      status: 1
    })
    models.value = resp.list || []
  } catch (error: any) {
    console.error('获取模型列表失败:', error)
    ElMessage.error('获取模型列表失败: ' + (error.message || '未知错误'))
  }
}
```

## 2. 前后端字段映射分析

### 前端表单字段结构
```typescript
const form = ref({
  id: undefined as number | undefined,
  name: '',           // 模型名称
  code: '',           // 模型编码
  parentId: 0,        // 父级ID
  description: '',    // 描述/设计目标
  type: 'business',   // 类型
  status: 1,          // 状态
  sort: 0,           // 排序
  remark: '',        // 备注
  readonly: 0        // 只读标识
})
```

### 后端期望字段（基于 DomainSaveReqVO）
```java
public class DomainSaveReqVO {
    private Long id;
    private String name;
    private String code;
    private Long parentId;
    private String description;
    private String type;
    private Integer status;
    private Integer sort;
    private String remark;
    private Integer readonly;
}
```

**字段匹配情况**：前后端字段结构基本一致，类型也匹配。

## 3. 人性化改进建议

### 当前改进点
1. **模型创建步骤**：使用更通俗易懂的语言
   - "第一步，给模型起个名字" 而不是 "第一步：我在做什么？"
   - 增加生活化的类比说明

2. **表单标签优化**：
   - "模型名字" 替代 "这个东西叫什么名字？"
   - "主要用途" 替代 "这是什么类型的概念？"
   - "设计目标" 替代 "这是做什么用的？"

3. **概念类型映射**：
   ```typescript
   const conceptTypes = ref([
     { value: 'entity', label: '👤 人物角色', tip: '比如客户、员工、供应商等' },
     { value: 'process', label: '🔄 业务流程', tip: '比如订单处理、审批流程等' },
     { value: 'asset', label: '📦 资产物品', tip: '比如设备、库存、产品等' },
     { value: 'data', label: '📊 数据记录', tip: '比如报表、统计、日志等' }
   ])
   ```

## 4. 建模字段需求

基于领域建模的需要，建议以下字段配置：

### 基础信息字段
- **name (required)**: 模型名称
- **code (auto-generated)**: 系统编码，支持手动修改
- **type (required)**: 概念类型（entity/process/asset/data）
- **description**: 设计目标描述

### 层次结构字段
- **parentId**: 父级模型ID（支持层次化管理）
- **sort**: 排序权重

### 状态管理字段
- **status**: 启用状态（1=启用，0=禁用）
- **readonly**: 只读标识（1=只读，0=可编辑）

### 扩展字段
- **remark**: 备注信息

## 5. 解决方案

### 立即解决方案
1. **确认后端 API 状态**：检查 `/system/domain/page` 接口是否正常工作
2. **数据库表检查**：确认 domain 相关表是否存在
3. **权限验证**：检查用户是否有 `system:domain:query` 权限

### 长期优化方案
1. **完善人性化语言映射**
2. **增强表单验证**
3. **优化用户体验流程**

## 6. 菜单配置参数

为实现设计自动生成页面时通过调用菜单管理API接口自动创建，需要以下菜单配置参数：

### 业务建模工作台菜单
```sql
INSERT INTO system_menu (
  name, permission, type, sort, parent_id, path, icon, component, status
) VALUES (
  '业务建模工作台', 'system:domain:query', 2, 2, [业务建模父菜单ID], 
  'workspace', 'ep:suitcase', 'domain-model/workspace', 1
);
```

### 对应的权限配置
```sql
-- 查询权限
INSERT INTO system_menu (name, permission, type, sort, parent_id, path, icon, component, status) 
VALUES ('业务建模工作台查询', 'system:domain:query', 3, 1, [工作台菜单ID], '', '', '', 1);

-- 创建权限
INSERT INTO system_menu (name, permission, type, sort, parent_id, path, icon, component, status) 
VALUES ('业务建模工作台创建', 'system:domain:create', 3, 2, [工作台菜单ID], '', '', '', 1);

-- 更新权限  
INSERT INTO system_menu (name, permission, type, sort, parent_id, path, icon, component, status) 
VALUES ('业务建模工作台更新', 'system:domain:update', 3, 3, [工作台菜单ID], '', '', '', 1);

-- 删除权限
INSERT INTO system_menu (name, permission, type, sort, parent_id, path, icon, component, status) 
VALUES ('业务建模工作台删除', 'system:domain:delete', 3, 4, [工作台菜单ID], '', '', '', 1);
```

## 7. 新增功能：设计目标列表管理

### 功能描述
为"设计目标"字段增加了列表管理功能，允许用户添加多个具体的实现目标，提升记录的结构化和清晰度。

### 功能特性

1. **目标列表显示**
   - 每个目标以独立的标签形式展示
   - 带有图标和删除按钮
   - 支持鼠标悬停效果

2. **添加目标**
   - 文本输入框支持回车键快速添加
   - 输入验证，防止空目标添加
   - 添加按钮仅在有内容时启用

3. **预设建议**
   - 当目标列表为空时显示常见建议
   - 点击建议标签快速添加
   - 预设目标包括：
     * 提升客户满意度
     * 提高工作效率
     * 降低运营成本
     * 增强数据准确性
     * 简化操作流程
     * 提升用户体验
     * 增强系统稳定性
     * 提高数据安全性
     * 促进团队协作
     * 优化资源配置

4. **数据存储**
   - 目标列表以分号（；）分隔的字符串存储在 `description` 字段
   - 编辑时自动从字符串恢复为列表
   - 保持与后端数据结构的兼容性

### 技术实现

#### 数据结构
```typescript
// 设计目标相关数据
const designGoals = ref<string[]>([])      // 目标列表
const newGoal = ref('')                    // 新目标输入
const goalSuggestions = ref([...])         // 预设建议
```

#### 核心方法
```typescript
// 添加目标
const addGoal = () => {
  if (newGoal.value.trim()) {
    designGoals.value.push(newGoal.value.trim())
    newGoal.value = ''
    updateFormDescription()
  }
}

// 删除目标
const removeGoal = (index: number) => {
  designGoals.value.splice(index, 1)
  updateFormDescription()
}

// 更新表单描述字段
const updateFormDescription = () => {
  form.value.description = designGoals.value.join('；')
}

// 从描述恢复目标列表
const restoreGoalsFromDescription = () => {
  if (form.value.description) {
    designGoals.value = form.value.description.split('；').filter(goal => goal.trim())
  } else {
    designGoals.value = []
  }
}
```

#### 样式设计
- 使用卡片式布局，增强视觉层次
- 蓝色主题配色，与系统整体风格一致
- 悬停效果和过渡动画，提升交互体验
- 响应式设计，适配不同屏幕尺寸

### 用户体验优化
1. **直观的可视化**：目标以标签形式展示，一目了然
2. **便捷的操作**：支持键盘和鼠标多种交互方式
3. **智能的建议**：预设常见目标，减少用户输入负担
4. **平滑的反馈**：操作即时生效，无需额外确认 